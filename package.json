{
	"name": "nuxt-app",
	"private": true,
	"type": "module",
	"scripts": {
		"build": "nuxt build",
		"dev": "nuxt dev",
		"generate": "nuxt generate",
		"preview": "nuxt preview",
		"postinstall": "nuxt prepare",
		"db:generate": "npx drizzle-kit generate",
		"db:migrate": "npx rizzle-kit migrate"
		"db:migrate": "npx drizzle-kit migrate"
	},
	"dependencies": {
		"drizzle-orm": "^0.40.0",
		"nuxt": "^3.16.0",
		"postgres": "^3.4.5",
		"vue": "^3.5.13",
		"vue-router": "^4.5.0"
	},
	"devDependencies": {
		"drizzle-kit": "^0.30.5",
		"tsx": "^4.19.3"
	}
}
